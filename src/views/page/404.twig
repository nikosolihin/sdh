{% extends "base.twig" %}

{% block content %}
  <div class="container Four-o-four">
    <h1 class="center">{{error.title|title}}</h1>
    <div class="center">{{error.description}}</div>
  </div>
{% endblock %}
